services:
  app:
    build:
      context: ../..
      dockerfile: docker/prod/Dockerfile
    container_name: upstream-squash-template-prod
    env_file:
      - ../../.env.prod
    restart: unless-stopped